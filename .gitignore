/target
/tools/target
Cargo.lock
*.DS_Store
Thumbs.db
.claude/settings.local.json
.env
opencode.json
.serena/
.desloppify/
tmp/
/.worktrees/

# Override global gitignore — track docs and dot folders in this repo
!*.md
!.mcp.json
!.claude/
!.claude/**
!.cargo/
!.cargo/**

# Re-exclude transient / machine-synced files inside the tracked .claude dir
# (these must come AFTER the !.claude/** negation above or it re-includes them)
.claude/scheduled_tasks.lock
.claude/skills/desloppify/

# Machine-specific synced agent MCP config (absolute paths, per-machine)
.codex/

# Throwaway screenshots at repo root (playtester / session artifacts)
/*.png
